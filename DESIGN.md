# 2.5D 俯视角足球游戏 — 设计共识

> 本文档记录立项阶段的全部决策**及其理由**。
> 理由比结论值钱：将来想推翻某条时，先读它的理由，确认当初的代价评估是否仍然成立。

---

## 1. 项目定位

**一场 3 分钟的完整比赛。** 有开球、进球、终场比分、可重开。做到"能发一个链接给别人，对方玩十分钟不觉得敷衍"的程度。

- 不是玩具原型（做完就扔）
- 不是长线产品（赛季/球队/联机/平衡性）

这条定位是所有取舍的最终裁判。任何功能问一句"它服务于这 3 分钟吗"。

---

## 2. 核心决策清单

### 2.1 范围

| # | 决策 | 理由 |
|---|---|---|
| D1 | **纯 AI 单机**，无联机 | 联机会倒逼确定性 tick + 输入同步或服务端权威，整个代码结构从第一天就不同；事后改等于重写。足球是高频物理 + 22 实体，是网络同步的地狱难度。 |
| D2 | 同屏双人留后门（不实装） | 多绑一套键位即可，几乎零成本，但不进 M1~M4 范围。 |
| D3 | 规则集：出界全套 + 越位 + 简化犯规 | 见 D4、D5。 |
| D4 | **必须做越位** | 承重墙，不是拟真装饰。没有越位，最优解就是长传吊给永久停在对方门前的前锋，D12/D13 精心设计的短传/直塞体系立刻死亡。实现极便宜：传球瞬间冻结一帧，比较接球人 x 与对方倒数第二人 x。 |
| D5 | 犯规只做"从背后/侧面撞人且没碰到球 → 任意球"，**不做牌** | 没有犯规成本，防守会退化成贴身乱撞，抢断失去博弈。牌/罚下对 3 分钟比赛无意义。 |
| D6 | 不做：点球、换人、体力、门将操控 | 均不服务于 3 分钟闭环。门将另见 D16。 |

### 2.2 表现形式

| # | 决策 | 理由 |
|---|---|---|
| D7 | **正俯视 2D + 球带 z 轴** | "2.5D 感"由球的抛物线 + 椭圆影子提供，成本几乎为零。排除等距投影（每球员 8 方向动画，美术量翻倍）和真 3D（与像素风冲突）。 |
| D8 | 球渲染：影子画在 `(x, y)`，球体画在 `(x, y - z*8)` | 唯一的高度表达方式，必须两者同时存在——只有球没有影子，玩家判断不出落点。 |
| D9 | **跟随球的镜头 + 前瞻偏移 + 右下角雷达小地图**（约 160×100） | 全场固定视野下球员只有 ~14px，看不清朝向，且上帝视角让跑位战术失去意义。镜头不要死死居中，朝球运动方向偏移。 |
| D10 | 雷达是**必需品**不是装饰 | 见 §3 的可视范围计算：纵向只能看到 34 米（球场宽 68 米），玩家永远看不到对面边路的队友。没有雷达，长传和直塞就是瞎蒙。 |

### 2.3 玩法与手感

| # | 决策 | 理由 |
|---|---|---|
| D11 | **粘球 + 松动**：球吸附到脚前固定位置，但有最大偏移；急转向/高速时球会短暂脱离，视觉上滚出半个身位再被拉回 | 纯粘球没有失控紧张感；纯推球（FIFA 式独立刚体）新手三秒丢球，且盘带调参能吃掉两周。取中间。 |
| D12 | **抢断 = 短促铲抢键，失败有 0.4 秒硬直** | 硬直是防守博弈的全部来源。没有硬直，玩家会无脑连按抢断键，防守变成无成本行为。 |
| D13 | **传球混合制**：方向决定扇形，扇形内按各键的评分逻辑选人 | 纯辅助型 → "不是我在踢，是游戏在踢"；纯手动型 → 一直传丢。混合兼顾上手与掌控感。 |
| D14 | 三种传球的本质区别是**目标筛选逻辑 + 弹道**：<br>短传 = 地滚，吸附最强<br>长传 = 高球，吸附最弱（更看方向和力度）<br>**直塞 = 传向队友前方的空位，而非脚下** | 直塞传脚下就等于短传，"直塞"这个键就不成立。传前方空位是它存在的唯一理由，也是唯一必须配合 D18 跑位的键。 |
| D15 | **切人 = 离球最近的场上球员**（不含门将），按一次切一个，按住不连切 | — |
| D16 | 传球出脚后**保持控制传球者 0.5 秒**，之后自动接管"预测接球点最近的队友" | 给玩家做"传完就跑"的空间。没有这 0.5 秒，传球瞬间操控权被夺走，正在进行的操作全断。这条让传球衔接顺滑十倍。 |
| D17 | **门将永久 AI 托管** | 门将是完全另一套逻辑（扑救、出击、开球门球），做好要另花一周，做差了每次丢球玩家都怪你。 |

### 2.4 AI

| # | 决策 | 理由 |
|---|---|---|
| D18 | **阵型锚点 + 全场平移**为主体：每位置一个归一化锚点 `(x,y) ∈ [0,1]²` + 一个"活动矩形"限制漂移范围。球位置归一化后，阵型朝球方向加权平移（离球近的人跟得多，后卫跟得少），进攻时横向展开、防守时纵向压缩 | 这是职业球队 AI 的实际做法：便宜，且**行为可解释**——球员做出奇怪跑位时你能说清为什么。 |
| D19 | 只在**离持球人最近的 2~3 名队友**身上叠一个极小的局部势场：在锚点周围采样十来个候选点，选「离最近对手最远 + 传球线路通畅 + 不越位」的那个 | 20 个人纯锚点、3 个人聪明，玩家感知到的就是"这队会跑位"。全场势场（influence map）聪明得多也贵得多，且**难以调试**。 |
| D20 | **传球评分函数输入项（全要，加权和）**：意图匹配（与输入方向夹角）、线路阻挡（线段到点距离 < 拦截半径）、推进价值、接球人压迫度、射程匹配、越位 | 「选谁」是传球好不好用的全部。 |
| D21 | **每种传球类型一套权重**：<br>短传 = 意图 60% + 线路 40%<br>长传 = 意图 70% + 射程 30%（长传本就该冒险，别帮玩家太多）<br>直塞 = 推进价值 50% + 线路 30% + 不越位（硬性否决）20% | 权重全部进 `tuning.ts`。 |
| D22 | **第一版全员属性数值完全相同** | 属性差异会**污染所有手感判断**：每次传丢你都要问「是评分函数写错了，还是这人传球属性只有 40」，调参从看得见的数学退化成玄学。等 M1~M3 手感锁定、你已知道"正常传球该是什么感觉"后再打开——那时才有一把可信的尺子。 |
| D23 | 结构上留好 `player.attributes`：**speed / passing / shooting / tackling，四项** | 四项足够，多了调不动。第一版全填同值。 |

### 2.5 技术

| # | 决策 | 理由 |
|---|---|---|
| D24 | **裸 TypeScript + Canvas 2D + Vite**，不用 Phaser / three.js | 项目复杂度 95% 在 AI 和物理，框架在这两块帮不上忙。Phaser 的 Arcade Physics 反而要绕开（你需要自定义 z 轴抛物线 + 22 agent 转向）。渲染压力约等于零：23 精灵 + 一张底图。引擎层约 300 行。 |
| D25 | **模拟层与渲染层彻底隔离**：sim 层是纯数据 + 纯函数，不 import 任何 canvas、不读 DOM、不碰 rAF；render 层每帧读 sim 状态画出来 | **这是整个技术栈的前提条件，事后补会很痛。** 做到这点，将来换 PixiJS/WebGL 是一周的活；做不到就是重写。Canvas 2D 的真正天花板只有两处：画面级特效（动态光影/辉光/大量粒子/天气）和改做真 3D——都是换渲染器，与 gameplay 无关。 |
| D26 | **sim 固定 60Hz tick + 累加器 + 渲染插值** | 手感必须**可复现**。变步长下 144Hz 和 60Hz 显示器上盘带手感、球速、抢断窗口全不一样，你永远分不清一次难受的抢断是设计问题还是掉帧。附赠：录像/回放/调试步进全部免费。 |
| D27 | 所有手感参数集中到 `tuning.ts` + 接 **lil-gui 运行时调参面板** | 这个面板会省下几十小时。 |
| D28 | **M1~M3 球员用纯几何图形绘制**：圆形身体（队服色）+ 小三角标朝向 + 椭圆影子，零美术资源。M4 再一次性换精灵 | M1 会反复修改碰撞半径、粘球半径、身体尺寸，每改一次重画精灵是纯浪费；更糟的是**好看的精灵会让你误以为手感变好了**（同 D22 逻辑）。"4 方向还是 8 方向动画"的决策也随之推迟到 M4——那时球员尺寸已冻结，换精灵是纯替换。 |

---

## 3. 硬数值

**像素风必须先定内部分辨率再定一切，反过来做会全盘返工。**

| 项 | 值 |
|---|---|
| 内部渲染分辨率 | **480 × 270** |
| 放大方式 | `image-rendering: pixelated`，**只允许整数倍**（2× → 960×540，3× → 1440×810）。非整数缩放会糊。 |
| 比例尺 | **1 米 = 8 内部像素** |
| 球员身高 | 1.8 m ≈ **14 px**（像素小人的舒适下限，再小画不出朝向） |
| 世界尺寸 | 105 × 68 米 = **840 × 544 内部像素** |
| 可视范围 | **60 × 34 米**（横向半个场；纵向只有球场宽度的一半） |
| 模拟频率 | **60 Hz** 固定 tick |

> 那个纵向 34 米是这套数字的**已知代价**：玩家永远看不到对面边路的队友。这正是雷达（D10）从锦上添花变成必需品的原因。

---

## 4. 键位

攻守共用一套键（同一个键，进攻是传球、防守是抢断），比分开两套好记。

| 键 | 进攻 | 防守 |
|---|---|---|
| **WASD** | 移动 | 移动 |
| **Shift** | 冲刺 | 冲刺 |
| **J** | 短传 | 切人 |
| **K** | 射门（蓄力） | 铲抢（不蓄力） |
| **L** | 直塞 | 呼叫协防（可后置/砍掉） |
| **I** | 长传 / 高球 | — |

- **射门必须蓄力**（按住时长决定力度），否则只有一种力度。
- **铲抢不蓄力**，按下即出——防守的高压节奏下蓄力会手忙脚乱。

---

## 5. 里程碑

### M1 — 一个人、一个球、一块场地 ← **当前阶段**

内容：粘球、跑、转向、冲刺、射门（蓄力）、球的 z 轴与椭圆影子、tuning 面板。
额外补两样（约 30 行）：**一个球门 + 进球检测**、**几个静止假人**当障碍物与参照物。
> 没有球门，对着空场射门毫无反馈，射门手感无从判断。

只实装 **WASD + Shift + K**，其余键位在本文档占位，不提前写。

**M1 是唯一不许赶的里程碑。** 这类项目 90% 的返工源于在错误的手感地基上盖楼。

**验收标准（四条全过 + 自己连续玩 20 分钟不觉得别扭）：**
1. 全速跑动中做 90° 急转，球会短暂脱离但能追回——既不"黏死在脚上"，也不"直接丢了"
2. 25 米外蓄满力射门，约 0.8 秒到达球门，能明显感到弧线和落点
3. 绕静止假人变向，不穿模、不卡住
4. 把 tuning 面板任一参数拖到极端值，能立刻说出它改变了什么——**说不出来的参数就是无效参数，删掉**

### M2 — 加 10 名队友

阵型锚点系统（D18）+ 局部跑位（D19）+ 三种传球（D13/D14/D20/D21）+ 雷达。仍然没有对手。
验收：验证"传球有没有目的地可传"——如果队友只站在锚点不动，短传/直塞就没有意义。

### M3 — 加对手 11 人

抢断、铲球硬直、球权切换、防守切人（D12/D15/D16）。这时它第一次像一场比赛。
M3 结束后：可以考虑打开球员属性差异（D22）。

### M4 — 规则与流程

出界 / 界外球 / 角球 / 球门球、越位、犯规任意球、开球、比分、终场、UI。
同期：一次性替换球员精灵（D28），并决定 4 方向 vs 8 方向动画。

---

## 6. tuning.ts 参数清单

M1 只需要「移动 / 粘球 / 球物理 / 射门 / 相机」五组，其余随里程碑逐步加入。

**移动**
`maxSpeed` `acceleration` `deceleration` `turnRate` `sprintMultiplier` `sprintAcceleration`

**粘球（D11）** — M1 实装后的最终形式，见 §9
`dribbleOffset`（球在脚前距离） `stickyRadius`（吸附半径） `maxDetachDistance`（脱离上限） `reattachRate`（修正增益） `maxCorrection`（修正速度上限） `touchAccel`（触球加速度） `shotCooldown` `detachCooldown`

**球物理**
`groundFriction` `bounceRestitution` `bounceFriction` `gravityZ` `airDrag` `curveFactor` `spinDecayAir` `spinDecayBounce`

**射门**
`chargeTime`（蓄满时长） `minPower` `maxPower` `loftAngle` `inheritVelocity`

**相机（D9）**
`lerp` `lookahead` `maxLookahead`

--- 以下 M2+ ---

**传球（D14/D21）**
`shortPassSpeed` `longPassSpeed` `longPassLoft` `throughPassLead`（提前量） `passFanAngle` `passFanRange` + 三套评分权重

**抢断（D12）**
`tackleRange` `tackleWindow` `tackleRecovery`（0.4s 硬直） `foulThreshold`

**AI（D18/D19）**
`formationFollowWeight`（按位置分档） `compactnessDefend` `widthAttack` `activeRunnerCount`（2~3） `runnerSampleCount`

**球权（D15/D16）**
`switchCooldown` `passControlHoldTime`（0.5s）

---

## 7. 明确的非目标

联机对战 · 同屏双人（留后门但不实装）· 点球 · 换人 · 体力 · 门将操控 · 黄牌红牌 · 赛季/球队/转会 · 音效（未评估）· 真 3D · 第一版的球员属性差异

---

## 8. 未决事项

- 球员精灵 4 方向 vs 8 方向动画 → **M4 决定**（尺寸冻结后）
- 球员属性差异何时打开 → **M3 结束后评估**
- `L` 键防守侧的"呼叫协防"是否实装 → M3 时决定，可砍
- 音效、UI 视觉风格、球场底图美术 → M4

---

## 9. M1 实施记录：设计文档没说对的地方

M1 写完并用无头测试台（`npm run sim`）量过之后，有三条决策需要修订。
记在这里，因为它们都是"照着原设计做，做出来是反的"的例子。

### 9.1 D11 粘球模型：纯位置追踪做不出"松动"

原设计只说了"球吸附到脚前，但有最大偏移"。照字面实现是一个位置 P 控制器
（球速 = 位置偏差 × 增益），结果是**球永远甩不掉**：偏差越大球追得越快，
形成无限强的负反馈。实测冲刺中 180° 掉头，人球峰值间距只有 1.07 m，从不脱手。

中间试过给球速加绝对上限，无效 —— 稳态冲刺需要 8.78 m/s，
转向跟踪需要 8.1 m/s，**两个量级相同，速度上限无法区分它们**。这是个死参数。

最终模型三件套：

1. **前馈**：球的目标速度 = 球员速度 + 修正量。稳态位置误差趋近 0。
2. **修正封顶**（`maxCorrection`）：脚最多能以多快的*相对*速度把球拨回来。
   没有它，脱手瞬间球会带着十几 m/s 飞出去，变成一脚二十米的大脚球。
3. **触球加速度**（`touchAccel`）：球有惯性，脚只能以有限加速度改变球的速度。
   **这一条才是"松动"的唯一来源** —— 急转时球带着旧速度冲出去，脚拉不住。

实测结果：慢跑 90° 急转不脱手（峰值 1.16 m）；冲刺 90° 急转脱手，
球滚到 2.77 m 外，追球 1.07 s 追回；冲刺 180° 掉头必脱手，4.14 m，1.33 s 追回。

### 9.2 `sprintAcceleration` 必须【高于】常规加速度

原 §6 注释写的是"冲刺是提速慢但极速高"，这是教科书做法。实测下来是反的：
冲刺加速度低 → 转向过程更平缓 → 球跟得更紧 → **冲刺时控球反而更稳**。
和"高速更容易丢球"完全相反。

所以改成 `sprintAcceleration (26) > acceleration (20)`：冲刺推得更狠、转得更急。
**冲刺的代价从"提速慢"移到了"急转必丢球"上。** 这是个更好的成本，
因为它是玩家能感知、能规避、能做取舍的。

### 9.3 弧线的 Magnus 项必须乘球速

原设计只说"弧线来自出球瞬间的横向速度"，没规定量纲。写成常数侧向加速度时，
曲率 = a/|v| 在球慢下来后发散：一脚 30 米射门实测拐出 26 米，是回旋镖不是弧线。
改成侧向加速度 ∝ spin × 球速后，spin=5 偏移 1.82 m，spin=12 偏移 4.45 m，量级正常。

配套加了 `spinDecayAir` 和 `spinDecayBounce`：落地后弧线要迅速消失，
否则地滚球会一路拐弯。

### 9.4 试玩发现：`touchAccel` 定错了，任何持续转向都会丢球

**症状**：带球带着带着球就没了。

**实测**（10 秒各种带球模式的控球率）：直线 100%，但**绕圈跑 8%、S 形变向 17%、
四方向乱点 6%** —— 也就是玩家一开始转弯，球立刻丢，而且转着转着永远追不回来。

**根因是调参方法错了**：`touchAccel = 24` 是只用「一次干净的 90° 急转」标定出来的，
从没检查过*持续*转向。而绕圈跑的向心加速度就要 20.4 m/s²（速度 6.5，转 3.14 rad/s），
上限 24 几乎被吃光，误差必然一路累积到脱手阈值。

**中途否掉的一条路**：试过用「球与球员的相对速度」代替位置误差做脱手判据
（想法是绕圈时球与人协同、急转时球带着旧速度冲）。实测发现相对速度被
`maxCorrection` 恒定钳死在 4.5 m/s，**所有模式读数完全一样，零区分度**。量了才知道，
省得白建一套机制。

**更根本的发现：位置误差这个信号分不开「急转」和「持续转」。** 实测量程互相重叠：
四方向乱点 2.24 > 冲刺 90° 急转 1.74 > S 形 1.48 > 绕圈 1.41 > 慢跑 90° 急转 1.32。
不存在任何一组参数能同时做到「持续转向安全」和「90° 急转丢球权」。

**修正后的读法**：D11 原文是「球滚出去半个身位**再被拉回**」—— 描述的是
*球仍在控制下、只是明显滞后*，不是丢失球权。把「脱离」实现成丢球权是过度解读。
现在 `touchAccel = 40`：绕圈平顺（甩出 3px），急变向球明显甩出（11px）但控得住，
**只有冲刺中 180° 掉头才真的丢球权**，且转身追 0.78 s 能追回。

`touchAccel` 的可用区间很窄：40 以下绕圈丢球，50 以上绕圈只甩出 2px、松动感消失。

**测试台的教训**：漏掉这个 bug，是因为原来只测了离散的单次动作。
现在 `npm run sim` 里加了 7 种**持续带球模式**（直线/斜向/画圆/S 形/四方向乱点 ×
慢跑与冲刺）各跑 10 秒，要求零脱手且控球率 > 99%。

### 9.5 第二次试玩发现：A/D 瞬间反向 —— 以及"自己丢球"这件事本身就是错的

**症状**：「我 A/D 一下就丢了」。

**又是一个测试盲区**：9.4 加的持续带球模式里，`四方向乱点` 是 右→下→左→上，
**相邻方向永远差 90°，从不差 180°**。而键盘上按一下 A 再按 D 就是瞬间 180° 反向，
一次按键的成本，是最常见的操作之一。同样漏掉的还有「冲刺急停后反向」。

**修正**：`maxDetachDistance` 1.5 → 2.2。实测这样所有键盘反向操作都不丢球，
而且比「调高 touchAccel」更好 —— 后者会把松动感一起压掉（60 时甩出 11px，90 时只剩 8px），
前者保留完整的 12px。

**更重要的是想清楚了 `maxDetachDistance` 到底是什么**：

M1 没有对手。**玩家自己把球弄丢，不产生任何玩法，只产生挫败感。**
带球的"代价"不需要靠丢球权来体现 —— 它已经由「球被甩出脚前 12px」这件事本身承担了：
等 M3 有了防守球员，那 12px 就是对方能捅到球的窗口。

所以 `maxDetachDistance` 在 M1 里被重新定位成一个**安全阀**而不是玩法机制：
调到玩家自己的任何操作都碰不到它。它到 M3 才真正变活。

这同时推翻了 9.2 里"冲刺的代价是急转必丢球"的说法。冲刺的代价现在是
**球被甩得更远、转弯半径更大**，这是个更好的成本 —— 它是连续的、可感知的、
而且到 M3 会自然转化成"被断球的风险"，不需要一个突兀的丢球权判定。

**测试台**：现在 12 种持续带球模式（含 4 种反向 / 急停），加一条硬断言
「玩家自己的任何键盘操作都不会丢球权」，外加「球被外力打飞后 2 秒内能追回」。

### 9.6 待你在 20 分钟试玩中裁决的一件事

`maxCorrection` 是这批参数里最弱的一个：从 1.5 拖到 100，
脱手瞬间球速只从 4.5 m/s 变到 6.5 m/s，而且 20 以上就饱和了。
它有明确的物理含义、也确实兜住了最坏情况，但按 M1 验收标准 4
（"说不出来的参数就是无效参数，删掉"）它处在及格线上。
试玩时留意一下你能不能感觉到它 —— 感觉不到就删。

### 9.7 M1 范围内的两处补充实现

- **调参面板独占右侧一栏**，不是浮在画面上。lil-gui 默认浮动会盖掉三分之一球场，
  而 M1 的全部工作就是一边看球场一边拖参数。
- **`window.__game` dev-only 调试句柄**（装配层，不在 sim 层）：
  让浏览器控制台和自动化测试能读到真实 sim 状态。

---

## 10. M2 实施记录

M2 = 10 名队友 + 阵型锚点(D18) + 局部跑位(D19) + 三种传球(D13/D14/D20/D21) + 雷达(D10)，仍然没有对手。

### 10.1 D19 的"推进"评分是个棘轮

`pickRunTarget` 第一版把推进项写成 `(候选点.x − 球员当前位置.x) × 权重`。
以球员**当前位置**为基准，就意味着"比现在靠前"每一帧都得分 ——
球员一路往前漂到活动矩形边界，锋线因此和中场脱节 25 米，整支球队被扯成两段。
改成以**锚点**为基准，并把权重从 1 降到 0.5。

顺带一提：D19 原本列的三项是「离最近对手最远 + 传球线路通畅 + 不越位」，
推进项是实现时自己加的。加自选项之前该先想清楚它会不会自激。

### 10.2 阵型基准点是"球在中圈时站哪"，不是"最深的防守站位"

第一版后卫基准放在 0.16（球场 17 米处），结果球在中圈时后卫线离中场 25 米，
中场往前一看谁都够不着，短传直接没有目标可选。
改成 DF 0.27~0.30 / MF 0.46~0.50 / FW 0.66 之后，球在中圈时
后卫 28-32、中场 51-53、锋线 63-64，前锋正好在中前卫身前 13-14 米。

### 10.3 地滚传球的力度必须反解，不能凑

第一版是 `速度 = 距离 × 摩擦 × 2.1 + 3.5`，凑出来的。33 米的直塞算出 41 m/s，
球能滚 72 米 —— 飞过接球人直接出边线，直塞成功率只有 65%。

指数衰减下有精确解：球速按 e^(-rt) 衰减，滚过距离 D 后剩余速度恰好是 **v₀ − D·r**。
所以想让球"到人脚下时还剩 arriveSpeed"，出球速度就是 `D·r + arriveSpeed`。
两个凑出来的参数换成一个有物理含义的，成功率从 65% 升到 100%。

### 10.4 "射程匹配"的甜点必须每种传球各有一个

第一版统一用 `maxRange × 0.55` 当理想距离，于是长传的甜点落在 25 米，
和短传选到同一个人 —— **三个键退化成了一个键**。
改成每种传球各带一个 `idealRange`（短传 13 / 直塞 20 / 长传 34），
从后场发起时短传选 14.6 米的人、长传选 28.2 米的人，三个键才真正不同。

### 10.5 D16 的控制权移交会把接球人变成雕像

D16 规定出脚 0.5 秒后就把控制权交给接球人 —— 但那时球还在飞，
而 AI 循环会跳过玩家控制的球员，于是接球人一拿到控制权就站住不动，球从他身边滚过去。

补了一条 `autoReceive`：**你不按方向键，他就自己跑向拦截点；你一按方向键，立刻听你的。**
这也正是 D16 想要的"衔接顺滑"。

同一个坑还有第二面：指定接球人一旦变成玩家控制的那个，AI 这边就【没有任何人】去追球了，
他要是没接到，球就永远躺在那儿。所以 AI 的追球人选择加了兜底：
接球人就是被控球员时，另派最近的人补位。

### 10.6 M2 没有对手，两处 D21 的规则暂时降级

- **越位**：D21 规定直塞要有"不越位"的硬性否决（占 20% 权重）。没有对手就无从判起，
  那 20% 暂时给了意图方向 —— 否则直塞完全不看你指哪边，会乱传。M3 加对手后改回硬性否决。
- **线路阻挡**：M2 里挡路的只能是自己队友，这一项要到 M3 才真正吃重。

### 10.7 测试台：M1 的物理要在"没有队友"的世界里量

加了队友之后，M1 那 32 项物理测试全都会被路过接应的队友撞歪。
测试台因此分成两种世界：`solo()` 只留被控球员，用于量物理；
`fresh()` 是完整 11 人世界，用于量阵型、跑位、传球。

### 10.8 M2 验收结果

DESIGN.md §5 给 M2 的验收就一句话：验证"传球有没有目的地可传"。
测试台从 4 个位置 × 5 个方向各发一脚，传完之后**不再给任何输入**，
全靠 autoReceive 和队友 AI 接球：

| | 成功率 |
|---|---|
| 短传 J | 19/20 = 95% |
| 直塞 L | 20/20 = 100% |
| 长传 I | 20/20 = 100% |

物理正确性 52 项全过。**但好不好玩仍然只有试玩说了算。**

---

## 11. 试玩反馈：「传球不够精准」

原话：*「最好不要完全根据我的方向来，稍微向队友那边偏移一点，传球能更精准。」*

球其实**一直是瞄着队友飞的**，不是瞄输入方向。所以先量，找真正的原因。

### 11.1 走错的一条路：加提前量反而更差

第一反应是"瞄的是队友此刻的位置，球飞 0.9 秒他早跑开了"，于是加了 `leadFactor`
按接球人速度预判落点。实测**结果反了**：

| leadFactor | 短传时接球人要跑 | 直塞时接球人要跑 |
|---|---|---|
| 0 | 2.6 m | 4.0 m |
| 0.85 | 4.0 m | 7.7 m |

原因：接球人不是靶子，他会主动跑向拦截点。把球往前送，只是让他多跑一截。
最终取 0.3 —— 只补掉出球瞬间的那点滞后，不做激进预判。

### 11.2 真正的原因：55° 的扇形是个硬性闸门，短传有 23% 的按键是哑键

换个假设量：在**带球跑动**中按传球键，有多少次根本没有球出去？

| 扇形半角 | 短传按键出球率 |
|---|---|
| 55°（原值） | **77%** |
| 75° | 86% |
| 90° | 95% |

方向差一点，扇形里就一个候选都没有，按键毫无反应。玩家不会知道是自己按偏了
还是游戏没响应，只会觉得"传球不好使"。这就是"不要完全根据我的方向来"说的那件事。

两处修改：

1. **扇形放宽到 90°，当宽松闸门用**。真正决定传给谁的是 `wIntent` 权重，
   不是这个硬性角度过滤 —— 闸门负责"别往后传"，权重负责"传给谁"。
2. **加兜底：按了传球键就一定有球出去**。一个候选都没有时，把球捅向输入方向的空档
   （距离取 `idealRange`），反正最近的队友会去追。真实球赛里也没有"不出脚"这个选项。

### 11.3 顺带修掉的一个更根本的问题：控制权没跟着球走

沿着"接到球的人对不对"这条线量下去，发现长传有 **1/3** 的情况球被另一名队友
半路接走，而玩家手里还攥着原本那个预定接球人 —— 球在一个 AI 脚下。
这个感受就是"传出去的球不听话"。

回到最初的需求原文：**「进攻就是持球队员」**。所以规则应该是无条件的：
谁拿到球就控制谁。加进 `updatePossession` 之后，三种传球
"接到球的人就是你控制的人"全部从 66~80% 变成 **100%**。

D16 的 0.5 秒保持窗口仍然有效 —— 那是球还在飞的时候的事；球一到脚，控制权就跟着球。

### 11.4 测试台的盲区（第三次同一类）

[11] 和 [13] 测的都是"全队站定后再传"，而玩家实际是**一边带球跑一边传**，
那时全队都在跟着球移动。上面两个问题都只有在这个场景下才暴露。

新增 [14]：4 种跑动方向 × 8 种传球方向 × 4 个时间点 = 128 次/每种传球，
断言两条 —— 按键必定有球出去（哑键率 0），且接到球的人就是你控制的人（≥98%）。

**三次试玩反馈，三次都是"测试只覆盖了静态/离散场景"。** 这是这个项目最稳定的 bug 来源。

---

## 12. 试玩反馈：「传的方向明明是这个，却被另一个队友拿到了」+ 力度条

### 12.1 上一轮的修复自己造成了一半问题

§11 为了消除"哑键"把扇形从 55° 放宽到 90°，代价就是方向的话语权被稀释。
实测「选中的是不是你指着的那个人」：短传 93%、**直塞 70%（最差偏离 75°）**、长传 83%。
直塞最差，因为 D21 给它的意图权重只有 0.2（越位那 20% 挪给了意图，推进却占了 0.5）。

改法不是把扇形收回去（那会让哑键回来），而是：

- 意图项从 `cos` 改成 **`(1 − 角度/扇形半角) ^ intentSharpness`**。
  cos 在小角度附近太平坦 —— 偏 60° 的队友只比正对的低一点点，很容易被别的项翻盘。
- 权重重排：三种传球的 `wIntent` 全部提到 0.9~1.0，直塞的推进项从 0.5 降到 0.35。

**闸门负责"别往后传"，权重负责"传给谁"** —— 这是这次学到的分工。

### 12.2 但真正的大头是"半路被截胡"，而我上一轮把它藏起来了

§11.3 用「接到球的人就是你控制的人」当指标做到了 100%。
**那个指标是有欺骗性的** —— 它只说明控制权跟着球跑了，
完全掩盖了"球给了另一个人"这件事本身。

换成玩家真正感受到的那个数（出球时瞄的是 A，最后球到了 B 脚下）：

| | 改前 | 改后 |
|---|---|---|
| 短传 | 7% | **3%** |
| 直塞 | 8% | **6%** |
| 长传 | **31%** | **15%** |

两处修改：

1. **接球人优先权**（`receiverPriority = 0.9`）：传球飞行期间，非指定接球人的吸附半径
   打一折。少了这一条，路过的队友会顺手把别人的球捞走。球一停下立刻解除 ——
   否则其他人会被永久压制，谁都捡不起这个球。
2. **长传仰角 32° → 22°**。更平更快：飞行 2.08 s → 1.67 s，最高点仍有 3.4 m（照样过顶），
   但留给"局面变化"的时间少了 20%，被截胡的概率随之大降。

长传剩下的 15% 不打算再压 —— 40 米高球被另一名队友接到本来就是正常足球，
而且现在控制权会跟着球走，玩家不会莫名其妙控着一个没球的人。

### 12.3 力度条：蓄力决定"传多远"，不决定力度 —— **这一版是错的，见 §13**

四个动作键（K/J/L/I）统一成一套蓄力状态机：按住蓄力、松开出球，同一时刻只能蓄一个，
先按下的那个说了算。

但**射门和传球的蓄力含义刻意不同**：

- **射门**的蓄力 = 力度（球速）。
- **传球**的蓄力 = **你想传多远**（在 `minRange`~`maxRange` 之间插值），
  球速仍然由 §10.3 的精确解反解到接球人脚下。

传球不让玩家直接控力度，是因为那会把辅助传球（Q5 定的混合制）变回手动传球：
力度稍微偏一点，球就从接球人身边滚过去 —— 恰好是玩家上一轮抱怨的"不够精准"。

于是三件事各管一件：**按键决定弹道类型、蓄力决定传多远、方向决定传给谁。**
`idealRange`（每种传球的固定甜点，§10.4 加的）随之被 `minRange` + 蓄力取代。

### 12.4 教训：小心自己造出来的指标

这一轮最值得记的不是任何一个参数，而是 §11.3 那个 100%。
**当一个指标做到 100% 而玩家仍在抱怨同一件事时，多半是指标选错了，不是玩家错了。**

---

## 13. 试玩反馈：「出球力度怎么没随进度条变化」

§12.3 的设计是：传球蓄力决定"传给多远的人"，力度由系统精确反解到接球人脚下。
理由（避免退回手动传球）是成立的，但它有个致命的交互问题：
**选中同一个人时，轻点和蓄满的球速完全一样。进度条填满了，画面上什么都没变。**

实测改前的球速：

| | 轻点 | 半蓄 | 蓄满 |
|---|---|---|---|
| 短传 | 12.2 | 12.0 | 18.0 |
| 直塞 | 16.3 | 16.1 | **15.7**（蓄满反而更慢） |
| 长传 | 18.6 | 18.4 | 18.1 |

**一个填满了却看不出效果的进度条就是坏的交互，跟背后的设计多讲究无关。**

### 13.1 改法：力度直接跟蓄力条走，系统只做小幅修正

新参数 `powerAssist`（0..1）：

- 0 = 力度完全由蓄力条决定，球正好飞你蓄的那个距离，传不传得到全看你自己
- 1 = 完全由系统反解到接球人脚下（§12.3 的旧行为）

出球距离 = `蓄力距离 + (接球人实际距离 − 蓄力距离) × powerAssist`。

**实测代价几乎为零**：即使 powerAssist = 0（力度完全交给玩家），
球到队友脚下仍有 97~100%。原因是【选人本来就是按蓄力距离挑的】，
蓄的力度和目标的距离天然吻合，两者很少打架。

既然没有代价，就把话语权更多给玩家：**默认 `powerAssist = 0.25`**。改后：

| | 轻点 | 半蓄 | 蓄满 | 提升 |
|---|---|---|---|---|
| 短传 | 10.1 | 13.4 | 18.4 | +83% |
| 直塞 | 13.1 | 17.0 | 21.0 | +60% |
| 长传 | 16.5 | 20.2 | 23.5 | +43% |

副作用：半蓄的长传会明显传短，球落地后要有人跑过去捡。
这是手动力度的正常代价（真实球赛里也有传短的长传），不是传丢 ——
测试台的接球窗口相应从 4 秒放宽到 8 秒。

### 13.2 又一个测试盲区：只测了"选谁"，没测"踢多用力"

§12 加的 [16] 验证了"蓄满会选更远的人"，就以为力度条做完了。
**从没有一条断言检查过球本身有没有踢得更用力。**

现在补上：三种传球都要求出球速度随蓄力条**单调上升**，且蓄满至少比轻点快 40%。

这是第五次同一类问题 —— 断言覆盖了机制的一半，漏掉的那一半正好是玩家能直接看见的那一半。

---

## 14. M3 实施记录：22 人对抗

M3 = 对手 11 人 + 铲抢 + 铲球硬直 + 球权切换 + 防守切人（DESIGN.md §5）。
两笔 §10.6 记下的账也在这一版还清了：**越位硬性否决**和**线路阻挡**。

### 14.1 还清的两笔账

- **越位**（D4/D21）：直塞的目标筛选现在带硬性否决 —— 出脚瞬间冻结一帧，
  接球点比"对方倒数第二人"更靠前就不传。D4 说得很清楚：没有越位，
  最优解就是把一个前锋永久停在对方门前一直吊球，短传和直塞体系立刻死亡。
  测试 [20] 就是照这个场景写的：门口挂机的前锋传不到，同一个人退回来就传得到。
- **线路阻挡**（D20）：现在有对手了，这一项才真正吃重。

### 14.2 阵型镜像与"防守回收"

`slotAnchor` 加了 `dir`（进攻方向）和 `hasPossession` 两个参数：
1 队的基准点整体镜像（`base.x → 1 − base.x`），丢球权时整条阵线朝自家球门回收 `defensiveDrop`。

**踩过的坑**：一开始把"回收"的条件写成"本方没有球权"，
结果球是自由球时（比如传球飞行中）全队也往后缩 9 米，接球人被自己的阵型拖走。
正确条件是**对方持球**才回收，自由球不回收。

### 14.3 防守三档

跟进攻一样，绝大多数人只是站在阵型锚点上，只有跟球直接相关的两三个人做局部决策：

1. **逼抢者** —— 离持球人最近的那个，跑向他的【球门一侧】（不从背后扑，那样一被转身就没了），够到了就出脚
2. **补位者** —— 站在逼抢者身后靠自家球门的位置，被过掉时还有一层
3. **其余人** —— 阵型锚点 + 往最近的对手身上拉一点（盯人）

新增 `pressRadius`（逼抢半径）：只有离持球人这么近的防守球员才会离开阵型。
这是防守强度最重要的一个数 —— 没有它，"对手多黏人"根本没法调。

### 14.4 铲抢：硬直是唯一的设计重点

按下即出、不蓄力（D12：防守的高压节奏下蓄力会手忙脚乱），
出脚开一个 0.22 秒的判定窗口，够到球就断下来，**落空吃 0.4 秒完全不能动的硬直**。

实测连按抢断键 7 秒只能出脚 12 次 —— 硬直 + 冷却把它限住了。
这正是 D12 的全部意义：没有硬直，防守就是没有成本的行为，也就没有任何博弈。

**踩过的坑**：判定顺序。一开始把铲抢结算放在出脚之前，
于是按键要等下一帧才生效，防守慢半拍。改成【先结算再扣时间】，
本 tick 刚出脚的那一下立刻参与判定。

### 14.5 只算对手的线路阻挡是错的

把 `laneClearance` 改成只看对手之后，无对手的世界里这一项变成常量、完全失效，
传球开始从队友堆里硬穿，"被半路接走"从 3% 涨到 10%。

**队友挡在线路上同样会截球**，只是后果轻一点（球给了错的人，但还是本方球权）。
最终两者都算，队友的等效距离放大一倍。

同样的错误还犯在 `pickRunTarget` 的"找空档"项上：
把队友权重降到 0.5，结果队友互相挤得更近，被自己人截胡的比例反而上升。

### 14.6 测试台：三种世界

M1 的物理必须在只有 1 个人的世界里量，M2 的传球必须在只有己方 11 人的世界里量，
M3 的对抗才用完整的 22 人。

理由是"加了对手之后，传球成功率下降"这句话没有信息量 ——
可能是传球坏了，也可能是防守正常工作。混在一起就什么都说明不了。

### 14.7 M3 尚未做的事

- **犯规/任意球**（D5）：铲抢目前只有"断到球"和"落空硬直"两种结果，
  撞到人不算犯规。任意球是 M4。
- **门将扑救**（D17）：门将站在门线上，靠身体碰撞挡球 —— 这是碰撞的副产品，
  不是真正的守门逻辑。
- **AI 传球**：对手持球时只会朝球门带，不会传。让 AI 会传球需要一整套威胁评估，
  而 M3 要验证的是"防守能不能把球断下来"，AI 一直带球反而是更干净的测试条件。

---

## 15. M4 实施记录：规则与流程

M4 = 出界全套 + 越位判罚 + 犯规任意球 + 比赛流程 + UI（DESIGN.md §5）。

### 15.1 死球状态机是这一版的核心

M4 加的不是"几条规则"，而是一个新状态：`phase = playing | restart | fullTime`。

**"playing"之外的一切都不判罚。** 少了这个闸门，一次出界会在同一帧被反复判罚。

为了 3 分钟一场的节奏，重开球一开始做了简化：主罚球员直接被放到球边上并拿住球，
对方被推出 6 米。**这个简化是错的，见 §17。**

另外：**球一被开出来就立刻恢复比赛**。`restartDelay` 只决定 AI 何时出球，
玩家主罚必须等待实际出球，不能用倒计时兜底解除保护。
所以快发任意球是有回报的 —— 对方还退在 6 米外。

### 15.2 越位是在接球时判的，不是传球时

`isOffside` 在两个地方用，含义完全不同：

1. **直塞的目标筛选**（M3 就有）：主动躲开越位的人，不往那儿传。
2. **判罚**（M4 新增）：出球瞬间冻结一帧，把此刻越位的己方球员记进 `offsideFlags`，
   **谁先碰到球就吹谁**。

第二条是必需的，因为短传和长传【没有】越位否决 —— 玩家照样可以硬往越位的人身上传。
只有筛选没有判罚，D4 说的"门口挂机"依然是最优解。

### 15.3 犯规：硬直只惩罚"没铲到"，不惩罚"铲的是人"

D12 的 0.4 秒硬直让抢断有成本，但它不区分"铲空了"和"铲到人身上"。
D5 补上后者：铲球判定窗口内撞到对手却没碰到球 → 任意球给对方，同时照吃硬直。

判定很简单：`lunge > 0` 期间，和对手的间隙小于 `foulContact` 就是犯规。
只在 0.22 秒的铲球窗口内检查，所以正常的身体接触不会被误判。

### 15.4 加了一个 `officiating` 开关

M1/M2 的测试全挂了 —— 因为球现在会真的出界，而一个直线带球 10 秒的测试
会跑出 80 米，必然过边线。

加了 `world.officiating`（练习模式：不判出界/越位/犯规），测试台的
`solo()` 和 `fresh()` 世界关掉它。规则本身由 `full()` 世界的 [22]~[25] 来测。

这延续了测试台一贯的分层：**每个里程碑的机制，在它被设计的那个世界里量。**

### 15.5 一次静默失败的编辑

给 `launch()` 加"记录越位球员"时，我匹配的那段代码在更早一轮已经被改过，
替换**没有报错、也没有生效**。typecheck 过、[20] 过（那测的是直塞筛选，走另一条路），
只有 [23] 失败时才暴露出来 ——「越位名单 []」。

教训：批量文本替换必须验证落点，不能只看 typecheck 绿了就算完。

### 15.6 试玩时值得注意的一件事

长传落地后还会滚很远（约 17 米），因为弹地保留了 82% 的水平速度
（`tuning.ball.落地水平保留`）。测试中一次 32 米的高球落在接球人附近，
却一路滚进了球门。M4 没有动这个数 —— 它是 M1 就定下的球物理，
改它会牵动射门和所有弹地手感。如果长传接不到，先拖这一个。

### 15.7 M1~M4 之后仍然没做的

- **门将扑救**：门将站在门线上靠身体碰撞挡球，这是碰撞的副产品，不是守门逻辑。
- **AI 传球**：对手持球时只会朝球门带，不会传。
- **球员属性差异**（D22）：结构留着，数值全相同。理由和当初一样 ——
  属性差异会污染手感判断。要开的话，现在是时候了。
- **球员精灵**（D28）：仍然是纯几何图形。换精灵需要 8 方向 × 跑动帧的美术资源。
- **换边、半场、点球、牌**：D6 明确排除。

---

## 16. 试玩反馈：「为啥球出底线了是中圈开球」

### 16.1 根因：两个互不知情的函数在判同一件事

M4 把底线上的判罚拆成了两个函数：

- `checkGoal` —— 判进球，条件里有一条 **`球必须无人控`**
- `checkOutOfPlay` —— 判角球/球门球，不关心球有没有人控

于是【带球过线】时：`checkGoal` 因为"球有主"直接放弃，球掉进出界判定 → 球门球。
明明是进球。反过来在别的角度就会出现"该给角球却给了中圈开球"。

**两个函数对同一件事各有一套条件，就一定存在它们互相打架的角度。**
底线上到底发生了什么，只能有一个决策点。现在合并成 `checkGoalLine` 一个函数：
先算过没过线，再算是进了还是出了，两条路互斥。

顺带修正了一条规则错误：**带球过线本来就是进球**，不该要求球无人控。

### 16.2 用过线瞬间的位置，不是过线之后的位置

球一个 tick 能走 1 米。用"过线后采样到的位置"判门框，会把擦柱而入判成出界、
擦柱而出判成进球。

现在在 `prevPos → pos` 之间按 x 命中底线的比例插值，算出**过线那一刻**的 y 和 z。
测试 [26] 里有一对对称用例：采样点和过线点分别落在门柱两侧，两种都必须判对。

### 16.3 修复反过来打中了测试台

带球过线现在算进球 —— 于是所有"持续带球 10 秒"的测试（冲刺 88 米）
一头撞进球门，世界重置，什么都量不到。

解法不是把测试改短（那会漏掉长时间累积的问题），而是给测试台加了**跑步机**：
球员跑出中圈 30 米就把【人和球一起】按同一位移平移回来。相对几何完全不变，
量到的粘球误差不受影响。

---

## 17. 试玩反馈：「不能闪回啊 出界了有个人去捡球呗」

§15.1 里我为了不拖时间，把死球重开做成了瞬移：主罚球员和球一起闪到开球点，
对方也闪着退开。省事，但画面上就是一堆人凭空闪现，**玩家根本看不懂发生了什么**。

我当时的顾虑（走过去主罚会拖十几秒）本身也站不住：
界外球和任意球的开球点【就在球出界的地方】，只有角球和球门球需要走一段。

### 17.1 现在的流程：三个阶段，全程零瞬移

| 阶段 | 发生什么 |
|---|---|
| `fetch` | 球停在它自己滚出去的地方，主罚球员**冲刺跑过去捡** |
| `carry` | 捡到了，带球跑向开球点 |
| `ready` | 球摆好，等待主罚球员实际出球后恢复比赛 |

配套的三条：

- **对方靠走的退出规定距离**，不是瞬移 —— AI 把锚点投影到 6 米圈外，自己走过去。
- **死球期间只有主罚球员能捡球**，否则对方会顺手把界外球捡走。
- **主罚球员由 AI 驱动，哪怕他正是玩家控制的那个。** 玩家不该被迫用自己的腿
  跑 20 米去捡球，那是纯粹的等待。球摆好（`ready`）才把控制权交还。

实测：一次界外球从判罚到恢复比赛，捡球用 1.78 秒，全程 0 次瞬移。
兜底是 `fetchTimeout`（6 秒）—— 球卡在角落或主罚的人被挤住时才会触发瞬移摆球。

### 17.2 又一次静默失败的替换

给 `ai.ts` 插入 `updateRestartAI` 时，我匹配的锚点字符串在更早一轮已经改成了多行注释，
替换**没报错也没生效**，直到 typecheck 报 "Cannot find name" 才暴露。

**这是 §15.5 记下同样教训之后的第二次。** 光"记下来"没用。
从这次起，凡是插入新代码块的批量替换，脚本里都跟一句 `assert` 检查落点。

### 17.3 测试台

新增 [27]，直接盯这次反馈：
阶段序列必须是 `fetch → carry → ready → playing`；
**任何球员单帧位移超过 3 米就算瞬移**（正常最快 8.8 m/s ≈ 0.15 m/帧），必须为 0；
死球期间对方碰不到球；球摆好后控制权回到玩家手上。

[22] 里原来那条"对方退到规定距离外"测的是瞬移推开，语义已经不成立，
换成了"判罚那一刻球还在出界的地方，没有闪到开球点"。

---

## 18. 试玩反馈：「队友捡到球后不会自动切换控制人员」

### 18.1 单点场景全是对的，靠整场扫描才找到原因

先按反馈复现了四个场景 —— 自由球远处队友去捡、球放在队友脚边、对手持球被断、
门将拿球 —— **全部正常切换**。单点测试到此为止会得出"没问题"的结论。

于是改成扫**整场的不变式**：模拟 180 秒，找出所有"我方持球但控制的不是持球人"的时刻。
结果是 0 段。但同一次扫描顺手量到了另一个数：

**一场 180 秒里，玩家有 17.3 秒（9.6%）被迫控制着门将。**

这直接违反 D17（门将永久 AI 托管）。而且更糟：门将一旦被玩家控制，
`updateTeamAI` 就会跳过他 —— 他杵在原地不动，球也不出去。
玩家的感受就是"控制权切到了一个不该切的人身上，而且他什么都不做"。

### 18.2 两处修复

1. **控制权永远不给门将。** 不是在每个赋值点各加一句判断，而是做成
   `ensureNotKeeper()` 每 tick 强制的**不变式**。
   前者是"我记得的地方都挡住了"，后者是"不可能发生"。
2. **门将拿到球会开出去。** 原来所有持球人都走 `driveCarrier`（朝对方球门带球），
   于是门将会一路带到中场。现在门将有自己的 `driveKeeper`：握 0.7 秒，
   然后朝本方最靠前的队友来一脚高球。

### 18.3 修复顺带炸出一个既有脆弱点

门将在 AI 循环**中途**把球开出去（`ball.owner = null`），
而同一轮循环后面的 `pickRunTarget` 还在用 `world.players[ball.owner!]` —— 直接崩。

`ball.owner` 在一轮 AI 里并不是不可变的，任何"中途改变球权"的行为都会踩到。
`pickRunTarget` 现在会检查球权是否还在，不在就退回纯锚点。

### 18.4 教训：单点测试证明不了"控制权跟着球走"

这类"某个状态永远该成立"的性质，**只能靠扫描证伪，不能靠举例证实**。
测试 [28] 因此不测单点，而是模拟一整场扫两条不变式：
玩家一秒都不该控制到门将；我方场上球员一拿到球控制权就在他身上。

实测：被迫控制门将 **17.3s → 0.0s**，场上球员球权一致性整场零违例。

---

## 19. 试玩反馈：「队友有点傻 不会自动防守」

### 19.1 AI 防守是完全失效的，不是"有点傻"

对照实验（玩家全程站桩，纯看两队 AI）：

| | 修前 |
|---|---|
| AI 铲球 | **0 次** |
| 球权易手 | **0 次** |
| 我方控球率 | **100%** |
| 防守方贴身距离 | 2.3 m |

180 秒里球从没易过手。**根因是几何上的矛盾**：

- 逼抢球员的目标点是「持球人身前 `markGoalSide` = 2.2 m」
- 出脚判定要求「离球 `aiTriggerRange` = 1.3 m 以内」

他站定的位置永远够不到球，于是永远不出脚。实测贴身距离 2.3 m，
正好卡在触发范围外 —— 这不是概率问题，是**结构性的必然**。

`markGoalSide` 是给【无球盯人】用的距离，我错误地让逼抢者也共用了它。
现在逼抢者有自己的 `pressGoalSide`（0.7 m），并且贴的是【球】而不是持球人。

修后：AI 断球 16 次 / 90 秒，球权易手 16 次，防守方能贴到 0.87 m。

### 19.2 顺带发现：AI 铲球是零成本的

修好逼抢之后，AI 铲球成功率是 **100%**（95 次出脚 95 次成功，0 次硬直）。

因为 `aiTriggerRange`(1.3) < `tackle.range`(1.5)：AI 只在必中的距离出脚，
所以永远不会落空、永远不吃 0.4 秒硬直。**D12 的"防守要有成本"只对玩家生效。**

实测 `aiTriggerRange ≤ 1.7` 时 AI 成功率都是 100%，**1.9 起才会铲空**。
定在 1.9，并在面板里标成"越大越容易铲空"—— 它同时是"对手有多黏"的总闸。

### 19.3 一个把我误导了的计数器

统计 AI 出脚次数时读到"0 次出脚、95 次成功"。
原因是**成功的铲球在同一 tick 内就结算了**，`lunge` 从 0 变成正数又立刻归零，
逐帧采样根本观测不到。改成用 `tackleCooldown` 的上升沿计数才对。

同一个坑还让 [18] 那条"疯狂连按抢断键"的断言误报：
它数的是 `world.tackles`（全场计数），AI 队友现在也会铲球，混进来就超标了。
改成只数被控球员自己的出脚。

### 19.4 教训：结构性失效不会表现为"效果差"

"队友有点傻"听起来像是要调参数。实际是一个**几何上不可能成立**的条件 ——
目标点在触发范围之外，无论怎么调 `markPull`、`coverDepth`、`pressRadius` 都不会有任何改善。

分辨方法很简单：先量"这件事发生过几次"。**0 次**从来不是调参问题。

---

## 20. 试玩反馈：「要有抢断和铲球，防守要自动切人，参考 FC26」

### 20.1 抢断与铲球分成两套数

原来只有一种出脚。现在拆成两个键：

| | 抢断 `K` | 铲球 `L` |
|---|---|---|
| 断球半径 | 1.5 m | **2.6 m** |
| 落空硬直 | 0.4 s | **1.1 s** |
| 铲到人的判定 | 0.25 m | 0.55 m（更容易犯规） |

**铲球的硬直必须明显长于抢断**，否则"够得更远"就是无脑更优解，
两个键退化成一个 —— 这和 §12.4「三种传球必须真正不同」是同一个道理。
测试 [30] 里有一条断言直接钉住这一点：铲球落空的代价必须是抢断的 1.8 倍以上
（实测 1.52 s vs 0.63 s）。

### 20.2 防守自动切人：用"谁最早够到球"，不是"谁最近"

一个背对球的后卫哪怕直线距离更近，也不该被切过去。所以判据是
`interceptPoint` 算出的**最早触球时间**（硬直中的人还要加上硬直剩余时间）。

两条约束，都是为了不把玩家转晕：
- **迟滞** `autoSwitchMargin`：新人选要比当前这个早 0.35 秒够到球才值得切
- **冷却** `autoSwitchCooldown`：切完压 0.45 秒

另外：**正在出脚的人不会被切走**（他已经做了决定），但**硬直中的会** ——
铲空了躺在地上还占着你的控制权是最糟的体验。

### 20.3 自动切人第一版直接破坏了 D16

球在空中飞的时候是无主的，自动切人于是在传球瞬间把控制权从传球者手里抢走 ——
D16 那条"出脚后保持控制传球者 0.5 秒（传完就跑）"当场失效。

修法是在自动切人里加一条：`passHold > 0 || receiver !== null` 时不切。
新机制接进来时，得去检查它会不会踩掉旧机制的地盘。

### 20.4 防守太黏：改成 FC 式的"先封堵、后择机"

§19 修好逼抢之后，**玩家拿到球 0.4 秒就被断掉** —— 因为逼抢者是"冲到球边立刻捅"。

改成两段式：
- 贴住之后要压满 `pressCommitDelay`（0.85 s）才敢出脚 —— 你有大约一秒出球或转身
- 但**触球一散开就立刻出脚**：球甩离脚前超过 `looseTouch`（1.1 m）不等冷却

第二条把防守直接接到了 M1 的粘球模型上：干净带球时球在脚前 0.5 m，
急转或冲刺变向会甩到 1.3 m —— **那一下就是防守该抓的窗口**。
干净带球是安全的，毛糙的一脚才会被惩罚。

AI 的铲球也收紧成"最后一搏"：只在持球人明显拉开距离**且防守者已经被过掉**
（持球人跑到了他和自家球门之间）时才用。少了第二条，玩家一加速逃离就会被从侧后方铲倒。

### 20.5 三条测试断言测错了对象

新机制上线后有三条老断言失败，**全都是测试写错了，不是代码坏了**：

1. **「带球跑开能拖住更久」** —— 它写的是 `moveX: 1`，那是朝对方球门跑，
   等于一头扎进对方防线。改成朝【离最近对手最远】的方向跑之后：
   原地 1.2 s vs 冲刺逃离 **14.7 s**。
2. **「疯狂连按抢断键」** —— 数的是 `world.tackles` 全场计数，
   AI 队友现在也会铲球，混进来就超标。改成只数被控球员自己的出脚。
3. **「按住 J 不放不会连续切人」** —— AI 队友现在真的会断球，控制权因此合法地变了。
   改成把球锁死在无人能碰的地方、并关掉自动切人来隔离按键的边沿检测。

**机制变强之后，原来"顺便成立"的断言会开始失败。** 每一条都要先问
"是代码退化了，还是这条断言本来就在量别的东西"。

---

## 21. 试玩反馈：「自动切换和手动切换没有平衡点」+「我没开球的时候不能抢」

### 21.1 切换的问题不是频率，是【手动切人根本没给玩家选择权】

先量了切换频率：一场 180 秒切 21 次/分钟，平均控同一个人 2.83 秒。
把 `autoSwitchMargin` 从 0.35 调到 1.5、冷却从 0.45 调到 2.0，
频率几乎不变（21 → 20 次/分钟）—— 因为**大部分切换来自球权变化，不是自动切人**。
这条路是死的，调这两个数解决不了问题。

真正的两个原因：

1. **手动切完只锁 0.45 秒**（和自动切人共用一个冷却），
   你刚挑好人半秒后就被自动切人抢回去 —— 手动切人形同虚设。
   新增 `manualSwitchLock`（2 秒），手动和自动用两个不同的数。
2. **`J` 永远切到"离球最近的那个"**，按两次和按一次没有任何区别。
   改成按【最早触球时间】排序、每按一次取下一个、到底绕回开头（参考 FC 的 L1/RB）。
   现在你想守某条传球线路时，可以一路按到那名球员。

### 21.2 「不能抢」：死球期间整个铲球模块被跳过了

实测对方开一次界外球，**玩家有 2.58 秒按抢断键完全无效**，
而且**按下去的那一脚会卡住 2.62 秒不结算** —— 因为 `updateTackleTimers`
整个被 `phase === "playing"` 挡在外面。

死球期间不能出脚是对的（对方还在退规定距离），但**计时器必须照常走**：

- 卡住的 lunge 要正常衰减掉，否则玩家按下的那一脚会挂在那儿
- **硬直要在死球期间消化掉**，否则比赛恢复的瞬间人还是僵的

现在只跳过【判定结算】和【发起新的出脚】，计时器每 tick 都跑。
另外死球等待从 0.9 秒降到 0.5 秒，判罚提示改成**贯穿整个死球**显示 ——
捡球加摆球要两秒多，不说一声玩家不知道自己为什么按键没反应。

### 21.3 又一次"测试测错了对象"

这一轮有两条断言失败，两条都是测试的问题，而且是同一个根因：
**AI 队友现在真的会把球断下来**，控制权因此合法地变了。

- 「防守时按 J 切到离球最近的球员」—— 契约已经变成"按最早触球时间轮换"，断言过时了
- 「手动切完自动切人要让开」—— 测量期间队友把球抢回来了，控制权跟着球走是对的

第二条的修法是把我方的逼抢和出脚全关掉（`pressRadius = 0`）来隔离。
**这已经是连续第三轮出现"机制变强后老断言开始失败"。**
每次都要先问：是代码退化了，还是这条断言本来就在量别的东西。

---

## 22. 试玩反馈（重复第二次）：「我没开球不能抢」

### 22.1 我第一轮修错了，因为我在猜他指什么

第一轮我把这句读成"对方开球期间按键无效"，修了死球期间卡住的出脚状态。
玩家原样重复了一遍 —— **说明修的不是他遇到的事**。

第二轮换了方法：不猜含义，直接量【按下抢断键的每一次分别被什么挡住】。

| 挡住的原因 | 占比 |
|---|---|
| **死球期间** | **52%** |
| 硬直中 | 18% |
| 上一脚还在判定中 | 16% |
| 冷却中 | 4% |
| 成功出脚 | 10% |

**一半以上的按键落在死球期间。** 玩家说的不是"某个按键坏了"，是**"比赛一直在停"**。

顺着量下去：一场 180 秒的比赛实际耗时 300 秒，**其中 120 秒（40%）是死球**，
54 次停摆，平均每次 2.21 秒。

### 22.2 三层根因，一层比一层深

**第一层：犯规判得太滥（29 次任意球 / 3 分钟）。**
原判据只看距离 —— 任何落空的出脚只要挨着人就吹。
加了一条"你是【撞】上去的还是伸脚够的"：朝对手方向的速度分量要超过 `foulSpeed`(5.5 m/s)。
站着伸脚（前扑 3.5 m/s）不算，11 m/s 冲进去的滑铲照样算。
任意球 29 → 14。

**第二层：修完第一层，死球反而涨到 74%。**
因为不再频繁停表，比赛继续进行，**角球+球门球暴涨到 66 次** —— 之前是犯规把它盖住了。
根因是断球时球被捅向**防守者的朝向**，而防守时人是面朝自家球门在封堵的：
**每一次成功防守都在给对方送角球。** 改成捅向本方进攻方向（真实的解围本来就是往外踢）。
死球 74% → 35%。

**第三层：捡球太慢。**
主罚人选按"离开球点多近"挑，但他第一件事是跑去捡球 —— 挑到了一个站在开球点旁边、
却离球二十米的人。改成按"离球多近 + 一点带球距离"挑，
并且球门球摆在离出界点最近的球门区位置（规则本来就允许），别让门将横穿二十米。
死球 35% → **18%**。

### 22.3 结果

| | 修前 | 修后 |
|---|---|---|
| 死球占实际时长 | **40%** | **11~19%** |
| 死球次数 / 3 分钟 | 54 | 22~36 |
| 每次死球时长 | 2.21 s | 1.80 s |
| **按抢断键被死球挡掉** | **52%** | **10~17%** |
| 实际出脚率 | 10% | 20% |
| 全场断球 | 24 | 46 |

### 22.4 两个方法论教训

**一、玩家重复同一句反馈时，别修第二遍，改成量。**
第一轮我修的是我以为的含义。第二轮我量了"每一次按键分别被什么挡住"，
答案（52% 死球）和我的两次猜测都不一样。

**二、单次模拟的方差会骗人。**
调 AI 铲球门槛时，任意球次数在 11~22 之间来回跳，看不出任何趋势 ——
我差点按噪声去调参。最后的做法是：保留原则上正确的改动（"去球不去人"），
但**不拿它调数**；测试阈值定在"能抓住真实退化"（40%/74%/52% 那一类）的水平，
而不是理想值 —— 卡死在理想值只会让测试乱闪。

### 22.5 一条断言的前提变了

[29]「AI 铲球会落空并吃硬直」现在失败：AI 贴到 1.15 m 才出脚，
那个距离在断球半径 1.5 m 之内，所以基本必中。

**这是设计如此，不是退化**：AI 的成本已经从"可能落空"换成了"必须先贴住你 0.85 秒"。
所以断言也换成玩家真正关心的那条保证 ——
**从被贴上到被断球，我有多少时间处理球**（修复前 0.4 秒，现在 0.98 秒）。

---

## 23. 试玩反馈：「守门员是摆设吗」

是的。我在 §14.7 和 §15.7 自己记过两次「门将只是靠身体碰撞挡球，不是守门逻辑」，
但一直没做。实测：**180 脚射正的球，扑出 0 次，75% 直接进网。**

### 23.1 门将现在做三件事

1. **封角度**：站在「球 → 球门中心」的连线上，离门线 `gkLineDepth`(1.6 m)
2. **预判落点**：球朝门飞时，算出它将要越过门线的那个点，横向移过去
3. **鱼跃**：预判点超出跑得到的范围时飞身扑救，扑完趴 `gkDiveRecovery`(0.8 s)

第 3 条同时是门将的**代价**：扑错方向就门户大开 —— 和铲球的硬直是同一个道理。

### 23.2 找到真正的瓶颈花了四次尝试

改完之后，180 脚射正**全部扑出**（0 个进球）—— 从一个极端跳到另一个极端。
接下来四次尝试，前三次都调错了地方：

| 尝试 | 结果 | 为什么没用 |
|---|---|---|
| 加反应时间 `gkReaction` | 仍然 100% 扑出 | 不是反应慢的问题 |
| 缩小鱼跃手长 1.9 → 1.25 | 仍然 ~98% | 不是够不够得着的问题 |
| 降低鱼跃速度 9 → 4 | 仍然 ~98% | **他根本不需要鱼跃** |
| **限制移动速度** | 终于有球进了 | ← 真正的瓶颈 |

真正的原因：**门将复用了场上球员的移动参数，冲刺速度 8.78 m/s。**
他不用扑，走过去就够到了 —— 所以反应时间、鱼跃手长、鱼跃速度这三个参数
**根本没有被用到**，调它们当然毫无变化。

新增 `gkSpeed`(3.8 m/s)。真实门将横向移动大约 4~5 m/s。

顺带修掉一个白送的便宜：**鱼跃一触发，接球半径就从 1.51 m 立刻涨到 2.46 m** ——
扑救范围是"宣布鱼跃"这个动作本身白送的，不是身体真的飞出去换来的。
现在 `gkDiveReach` 只比 `gkReach` 大一点点，覆盖范围来自 `gkDiveSpeed` 的位移。

### 23.3 我的测试也在骗我

第一版测试里设了 `ball.stickyLock = 999`（本意是防止队友捡球），
**这恰好禁止了门将接球** —— 所以「门将拿住」一次都没出现，
我看到的所有"挡下"其实全是身体碰撞。数字对了，含义完全错。

另外第一版的瞄准点最多只偏 ±3.0 m，而球门半宽是 3.66 m —— 全是好扑的球。
换成贴柱的 ±3.4 m、距离 12/18/26 m 之后，转化率才落到真实区间。

### 23.4 结果

| | 修前 | 修后 |
|---|---|---|
| 射正被扑出 | **0%** | **71%** |
| 射正转化率 | 75% | **29%**（真实足球约 30%） |
| 打角度能否打穿 | — | 能 |

### 23.5 教训：参数没反应，先怀疑它有没有被用到

连着三次调参毫无变化，正常的反应是"再调大一点"。
正确的反应是**去确认这个参数到底有没有进入计算路径**。
`gkReaction`、`gkDiveReach`、`gkDiveSpeed` 三个参数当时都是死的 ——
因为门将走路就够到球了，那三条分支从来没被执行。

**调参没有任何反应，多半不是数值不对，是那条代码根本没跑。**

## 24. 整体完成度完善：从测试台到可直接上手的比赛

这轮沿用 Canvas 2D、固定 60Hz 和现有传球 / 规则体系，补齐产品流程：

- 默认隐藏 F1 调参面板和调试 HUD，让球场获得完整宽度；比分、计时和操作提示由清晰的 DOM 界面承载。
- 增加赛前说明、时长选择、暂停与继续、失焦自动暂停、赛后比分和双方数据。
- UI 的 menu / playing / paused / results 与模拟的 playing / restart / fullTime 分开。暂停停止调用 step，不挤入死球状态，也不消耗比赛时间。切换界面清除按键和未完成蓄力，避免恢复时误射。
- 键盘短按缓冲直到模拟 tick 消费，修复两个 tick 之间按下又松开导致短传无效的情况。
- attack.ts 增加可解释的 AI 传射：观察后决策，传向未越位且线路开放的队友，禁区附近瞄准再射；没有合适机会继续推进。传球共用 launch，球速、越位和统计没有第二套实现。
- 对手传球的 receiver 不再屏蔽玩家防守自动切人；控制权移交只能指向我方。
- 双方统计在实际出脚 / 控球处累加，进球重开不清零，完整重开才清零。
- 控制球员标记、接球人标记、跑动摆臂迈步，以及取球 / 摆球 / 等待出球提示。
- 音效仅在表现层观察状态变化，用户开始后启用，无外部音频请求。

完整比赛回归测试验证能到终场；原有防守锁定测试隔离 AI 传射引起的合法球权变化，仍保留其时间阈值。新增传射与控制权检查见 scripts/match-regression.ts。已知边界仍为桌面键盘单机，不扩展赛季、联机、触屏操作。


## 25. 真实对抗中的扑救、抢断、传球与协防修复

用户反馈门将不扑救、抢不到球、传不到队友以及协防不足。复现必须走完整 step 链，并观察动作后的球权；只看“owner 曾变成门将”或 tacklesWon 加一会给出错误结论。

- 接球以前没有消除来球速度。门将接到约 28 m/s 的射门后，普通带球的有限触球加速度仍让球冲进球门。现在接球统一卸力，门将持球使用抱球位置；鱼跃结束、落地恢复后才开球。鱼跃可达范围按门将自身跑速估算，位移不再被普通停步逻辑提前刹住。
- 站立抢断以前一律捅成自由球，原持球人常马上捡回。现在干净的站立抢断直接收球，对方短暂失衡；滑铲仍解围并保留恢复代价。低空的对方松动球也能在出脚窗口内被争下，抱在门将手里的球不能踢走。
- 传球以前选中了队友，却按蓄力距离为主来计算力度。24 米轻点短传会差约 5.5 米。现在蓄力选人，实际目标决定力度；无人接应才使用蓄力指定的自由传球距离。长传解算加入空气阻力，双方传球使用跑动提前量，并补上本方半场不越位的判断。
- 协防以前追当前位置、提前减速，且可能继续分配倒地球员逼抢。现在预测下一步突破位置，贴近时保持追击速度，跳过倒地队友；补位跟住横向突破并保留纵深。

回归证据：9 个 AI 带球后射门场景由 0 次真正保住球变为 9 次，其中 7 次出现鱼跃；这组场景不能代表整体扑救率。另有 180 个不同距离、角度、力量、高度的射门场景，178 脚射正中约 78% 被阻止，仍有角度球进网。接球后继续模拟至少一秒，避免把接触误报为扑救。

3 次侧面贴身站立抢断由 0 次拿到球变为 3 次；3 种传球 × 3 种跑动速度场景都能接球并保持半秒；倒地后接管逼抢和冲刺突破协防另有独立场景。原来要求“选中同一队友也必须越蓄越快”的断言改为验证无人接应时的自由传球射程，定向传球改验实际送达。

这些结果是固定场景的回归证据，不能替代真人试玩的手感评估。


## 26. 防守切人以玩家跑位为先

用户反馈刚跑两步就被自动切走。复现发现两条独立路径：autoSwitchDefence 在对方带球时每帧比较候选人，旧冷却仅 0.45 秒；gainPossession 在对方每次接球时又强制选最近后卫，绕过 switchLock。手动 J 从非最佳球员开始轮换时，也可能跳到更远的人。

参考 [EA FC 26 官方切人设置](https://www.ea.com/able/resources/ea-sports-fc/fc-26?isLocalized=true)：默认自动切人条件为 Air Balls and Loose Balls，并有 Manual 等选项；移动辅助是独立设置。此处借鉴触发条件，不声称复刻 FC 26 内部算法或具体时间参数。Konami 的 [eFootball 官方更新说明](https://www.konami.com/efootball/en-us/page/2023/season2_patch-notes_v2-20) 也区分 Assisted / Semi-Auto，具体版本行为不同，不能概括成所有实况防守都绝不自动切人。

实现：

- 默认辅助只在 ball.owner 为 null 时选择防守人，对方持球时保持当前球员。
- 输入必须由 step 显式传入切人入口。持续方向输入以及松开后 0.35 秒保护跑位；这项与 1.2 秒自动切人冷却都是本游戏的选择。
- 一次无主球争夺只辅助切一次；手动选择后把本次争夺标记为已处理，避免 2 秒保护结束后切回。接球/死球重置机会，完整重开清除所有切人状态。
- 对方接球不直接改 controlled。本方场上球员接球仍按进攻逻辑移交，误控门将仍有独立兜底。
- 单次 J 优先选最早能接近球的队友，当前已最佳则选下一人；0.7 秒内连按继续轮换，按住不连切。
- 赛前和暂停均有辅助 / 手动选择，两处同步；切换设置不重置比分和比赛时间。

新增 scripts/switching-regression.ts：通过真实 step 复现并验证跑位第一帧失控、对方接球绕过保护、J 选远处队友三处错误；另覆盖地面与空中无主球、停止移动缓冲、手动模式、同次争夺锁定和本方传球移交。原旧用例“对手带球时必须自动切人”按新需求替换；手动轮换用例隔离实际抢回球后的进攻移交。


## 27. 防守切人必须对应当前可见球员

用户再次反馈切到了画面外。原第 26 节只解决“何时切”，仍按全队候选轮换；快速连按第三次即可选到另一侧边路。高球预测在 2.4 秒范围内追不上时统一返回 2.4 秒，造成远近球员同分、以数组顺序选人的第二个问题。

参考 [FC 26 官方设置](https://www.ea.com/able/resources/ea-sports-fc/fc-26?isLocalized=true) 的 Closest to Ball / Goal Side 和 Next Player Switch Indicator，以及 [eFootball 官方操作说明](https://www.konami.com/efootball/en-us/page/mobile_controller) 的按方向选择附近球员。以下是适用于本项目固定视野的实现选择，并非声称这些游戏都禁止切到屏幕外：

- main 在每次绘制前，用实际 Camera 偏移生成 DefenderSelection；下一次 step 使用这份显示过的选择。模拟层只接收世界坐标区域和候选索引，不读取显示设备，也不依赖渲染模块。
- control-view 使用与绘制一致的取整偏移，留出球员身体和头顶标记边距，排除小地图覆盖的区域。手动和辅助防守切人共用可见性约束；没有合格队友就保持原球员。
- switching.ts 集中候选筛选和排序。地面球按距球距离为主，高球按接近落点时间为主；预测范围外继续计算剩余距离。转身、现有跑动和球门侧站位只作小幅修正，倒地与门将不进入候选。
- 连按只在附近最多三人中轮换，替代此前全队循环。空心白三角和 J 提示下一人；J 确认刚显示的索引，若对方已跑出视野或倒地则取消本次选择，不暗中换给另一人。已控制者使用实心绿色标记，传球接应者仍使用黄色框。
- 本方实际接球、传球移交与门将兜底仍按各自规则处理，可见性约束针对防守切人。

scripts/visible-switching-regression.ts 先复现连续三次 J 切出视野和高球预测同分选远人，再通过实际 Camera → 选择快照 → step 链验证；另覆盖边界、更近的画面外候选、小地图遮挡、镜头移动、标记与动作一致、标记失效、球门侧权重和地滚球不提前跳到远处。原有“连按至少经过四名球员”断言按新局部候选规则替换，其余跑位保护和比赛规则继续回归。


## 28. 传球必须比带球推进快

用户反馈球刚出脚还没有球员跑得快。通过 firePass → updateBall 测量纯飞行，再用真实 step 验证冲刺轻传，确认旧短传到达速度 5 m/s、直塞 6.5 m/s 低于冲刺 8.775 m/s。旧 4 米轻传初速约 6.7 m/s；冲刺中 8 米轻传出脚后 0.2 秒，人球距离从 0.915 米缩为 0.861 米。

- 短传到达速度改为 11.5 m/s，直塞为 13.5 m/s。保留按实际目标距离反解初速的方式，跑动提前量使用同一到达速度参数；保留原有地面减速、跑速和射门参数。
- 单独提高球速后，3～6 米近传反而穿过接球人：原 stickyLock 将所有人的接球一起锁了 0.35 秒。现在 pass.launch 标记 pickupBlockedPlayer，仅阻止传球者接回自己的刚出脚球，队友可立即接球，对手仍可截球。过期、接球、重开清除标记；射门、碰撞弹球、带球脱离和铲球仍保留各自原有的全员冷却。
- 整场模拟暴露了背身接球后的卡位：球门侧逼抢目标落在持球者身体内，防守者反复碰撞、空抢，90 秒球权只易手 4 次。防守者现在在目标被身体挡住时绕到球侧，保留原抢断射程与贴防等待。新增背身、左右微偏三种真实 step 场景先复现再验证修复，未降低整场测试阈值。

固定场景实测：12 米短传到接球区域由 1.28 秒变为 0.70 秒；冲刺轻传后 0.2 秒人球距离由 1.020 米增加到 2.154 米；3 / 4 / 6 米短传分别在约 0.05 / 0.13 / 0.27 秒被接住。新增 scripts/pass-tempo-regression.ts 覆盖 4～20 米短传与直塞的全程球速、跑动出脚距离、近传收球，以及出球人不能回吸但对手仍能拦截。原三种传球接跑动队友并保持半秒的测试继续通过。

这些速度对比与到球时间是本游戏固定测试场景，不能替代真人连续传切、带球与防守的手感反馈。


## 29. 稳定控球与我方门将持球操作

用户明确要求正常带球不丢、停球少失误，并允许操作拿球的门将。这次需求覆盖此前“门将永久 AI 托管”的约定：无球仍自动守门，持球交给玩家。

先复现了侧后方停球的实际失误：球距身体 1.5 米时进入接球范围，但球在脚前目标的反方向，下一 tick 偏差超过 2.2 米，立即被旧脱球分支释放。8 个方向来球接下后连续 10 秒急转、冲刺和急停的用例在修复前失败。现在第一脚同时卸掉动量、把球收进身体附近；带球保留有限加速度与惯性，超过补脚范围时修正位置和速度，不再仅因方向输入主动丢弃球权。抢断、主动出球、越位和出界照常结算。

syncPossessionControl 统一我方活球持球与摆球完成后的控制权，包括门将；取球摆球仍由 AI 完成。门将无球后优先交回指定接应队友，没有目标时交给附近场上队友。对方接球不会强制重选后卫。

我方门将抱球后等待玩家发球，WASD 在禁区内以门将速度移动，J / L 发地面传球，I / K 发高球；K 发球计入传球，不计作射门。鱼跃位移和起身恢复仍完整推进，期间不提前出球，也不积存按键导致起身后误发。球门球摆好后同样交给玩家，实际松键出球才恢复比赛。对方门将保留自动发球。

新增 scripts/possession-control-regression.ts 的首批 8 项回归先全部失败，再验证通过：多方向接球后持续带球、门将接管与等待、移动边界、四个出球键、出球后恢复场上控制、鱼跃起身以及我方球门球。原测试中的“门将永远不可控”和“我方球门球必须自动发出”按新要求更新；整场 AI 防守观察用例增加玩家主动给门将发球的输入，避免将合法等待误判为防守不工作。


页面实战还发现对手仍会围着抱球门将逼抢，导致原地短传刚出手就被贴身截下。增加第 9 项回归先复现，再把门将抱球期间的对方场上球员改为退回阵型，并在目标站位上留出 4 米发球空间；出球后恢复普通争抢，没有给传球附加拦截保护。


本轮最终验证：56 项独立回归通过，生产构建和分层检查通过。整场模拟 138 项中有 1 项仍未通过：固定输入机器人每 0.1 秒尝试抢断时，601 / 1451 次按键落在死球期间（41%，旧阈值 25%）；同场死球时间占比为 28%（通过 30% 阈值）。保留原断言和失败结果，没有修改阈值或将其当作全部通过。该节奏指标仍需后续单独调查。


## 30. 长传落点接应与可设置按键

用户反馈队友接不到长传，并要求自行设置操作键。长传复现走 firePass → step：20 / 30 / 40 / 44 米、静止与横向 ±4 m/s 共 12 种情况。原来 30 米要追到约 4.5 秒才收到，40 米要 5.7～5.9 秒，44 米在 6 秒内收不到。interceptPoint 只比较水平位置，即使球仍在头顶也认为可接；空中采用 airDrag 的外推还会忽略着地后的弹跳和水平减速。现在高球分支使用 updateBall 按固定步长推进，只在高度不超过 0.5 米时认可拦截时间；未修改长传力度或扩大接球半径。12 种场景均在约 1.2～2 秒接住并保持至少半秒。地滚球原有闭式解保持。

按键配置位于 input/bindings.ts，保存物理 KeyboardEvent.code。Keyboard 在真实事件入口查询配置，保留两帧间轻点缓冲；改变配置、失焦、进入设置时清空旧长按和动作。重复键互换，左右 Shift 视为同一动作，不录入系统组合键；字母、数字、方向、空格、Shift、F1 可选，Esc 固定保留为暂停 / 退出。

KeySettings 使用原生模态框管理焦点。比赛中打开先暂停，录键阶段拦截事件，关闭后保持暂停由玩家继续。更改即时保存到本地存储；无效或损坏配置回默认，浏览器不允许存储时明确告知本次页面仍有效。恢复默认也同步保存。

所有页面键位说明、动态攻守提示、门将发球、主罚提示、手动切人选项、画布无障碍说明和下一位球员标记均从当前绑定读取。新增 6 项键盘回归从真实事件入口驱动模拟：长传改空格后旧 I 不响应，轻点空格实际触发高球；另覆盖冲突、方向、失焦、录键隔离、恢复默认、存储容错及提示。页面内实际验证了改键保存、旧键不出球、新键开出长传。

最终检查中 63 项独立回归通过。整场模拟仍保留上一轮未解决的死球按键占比失败，本轮为 495 / 1297（38%，阈值 25%）；没有因本次长传或改键工作修改该阈值。


## 31. 攻防分别改键与恢复参数入口

用户要求进攻、防守按键独立设置，并反馈原球员设置入口不见。当前代码找到的是原有速度、AI 和比赛参数 GUI，先按这一旧面板恢复右上角「球员参数」入口与面板内「收起」按钮；没有凭空声称存在参赛名单或阵容选择器。

按键模型增加 switchPlayer / tackle / slide，设置界面按进攻、防守、通用分组。冲突规则按同时可用的动作判断：攻防可以复用，同组内和通用键冲突仍交换；修改通用键时检查两个组的全部受影响动作。v2 保存格式允许跨组重复；首次读取旧 v1 时，将原共享键复制到相应防守动作，之后独立修改，保留用户原有配置。

Keyboard 按当前持球状态将实际物理键映射到模拟动作。额外携带 actionPressed 真实按下沿，避免从进攻转防守时，旧射门键仍按住导致新按的抢断键被吞掉；没有这些沿信息的旧模拟输入仍使用原逐帧边沿推导。默认短传和切人都是 J，但设置、底部提示、球场下一位标记与手动切人选项都分别读取各自配置。

新增独立射门/抢断、跨组复用、独立切人真实事件 → step、旧配置迁移、通用键多重冲突和攻守转换吞键回归；吞键场景先失败后修复。完整独立回归共 69 项通过，类型检查、构建和分层检查通过。页面验证了射门改 Q 后防守抢断仍为 K，以及原参数面板从常驻按钮展开；临时测试键位恢复默认。
